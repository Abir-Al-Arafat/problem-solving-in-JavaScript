// write a program to print "gold" or "silver" based on customer points using ternary operators
// if a customer has more than 100 points then its "gold" otherwise "silver"

let points = 100;

// ternary or conditional operator to check if the customer is gold or silver

let customer = points > 100 ? "gold" : "silver";

console.log(customer);